# -*- mode: ruby -*-
# vi: set ft=ruby :
# To use, install vagrant at http://vagrantup.com/

Vagrant::Config.run do |config|
  config.vm.box = "ec2-precise64"
  config.vm.box_url = "https://s3.amazonaws.com/mediacore-public/boxes/ec2-precise64.box"
#  config.vm.provision :shell, :inline => "/vagrant/script/install"
end
