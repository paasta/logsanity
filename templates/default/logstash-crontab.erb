#!/bin/bash
set -e
num_days=<%= node['logstash']['days_to_keep'] %>
index="$(date --date="@$(expr $(date +%s) - $num_days '*' 24 '*' 60 '*' 60)" +%Y-%m-%d)"
curl -f -s -o /dev/null -XDELETE http://127.0.0.1:9200/$index
